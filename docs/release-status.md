# 实现与验收状态

公开版本为 **v1.1**，工程版本为 `1.1.0`，整合此前 `0.1.0-rc.1` 开发交付。发布流水线须通过 Linux、Windows、macOS 工程验证后才创建 GitHub Release；这不将下述真实模型／宿主生产验收改为通过。历史报告中的旧版本及结论保留其原有语境。

本候选包含四个完整 Skill、三个真实原生入口、method-v5 两阶段编排、事件/时间管理、来源锁、事实与解释检查、授权包外状态、知识卡、转换接口、回归测试、四包构建及模型评测接口。

当前验收以 reports 中绑定当前源码的机器记录为准，不按本文字声明通过。`release-gate --local` 与完整 gate 的范围不同：前者只检验本地工程，后者还要求真实模型/宿主矩阵；历史审计不可逐项复现单独披露，不冒称新版测试等同于旧审计。

尚不能标记完成的外部验收：原 468 项审计测试源码与原始逐项复现；真实独立语言模型/宿主行为矩阵；跨平台产品安装；已审查外部真太阳时转换来源。接口、合成测试或 CI 配置不替代这些证据。不得写成“P0 已全部验收”或“所有环境稳定”。

W0 的完整 53 文件基线已恢复，新回归测试可复现；旧审计复现保持单独未知。W1、W2、W3 的本地代码路径与故障测试有记录。W4 提供四包及本地 ZIP 验证，真实模型矩阵待运行。W5 的 102 张简短知识卡仅完成来源/契约整理，不是完整术数断语库。W6 提供受审查 provider 接口，默认无来源即 unresolved。

交付不包含真实个案、密钥或聊天记录。本地修订源码完整包含于开发工程，远端操作是否推送必须以单独实际回执为准。